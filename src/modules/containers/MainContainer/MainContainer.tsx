import { AppMainLayout } from "../../../base/components"
import { MainComponent } from "../../components"

export const MainContainer = () => {
    return (
        <AppMainLayout>
            <MainComponent/>
        </AppMainLayout>
    )
}